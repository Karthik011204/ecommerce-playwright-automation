# ecommerce-playwright-automation
My project follows Page Object Model. Each webpage has a separate class, and the test file only calls those methods. I automated login, product selection, cart price validation, checkout, order verification, and logout. I also added API testing using Python Requests and Postman for CRUD operations.
